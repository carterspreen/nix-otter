# nix-otter
Just drop your assembly source in /src, cd into the repo, and run "nix build". 

The assembled binary will be symlinked in /result, along with a hexadecimal .mem that can be loaded with SystemVerilog
