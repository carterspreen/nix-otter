# nix-otter
Just drop your assembly source in /src, cd into the repo, and run "nix build". 

The assembled binary and will be symlinked in /result, along with a .mem file for loading with SystemVerilog.
